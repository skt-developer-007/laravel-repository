<?php

namespace App\Repositories;

use App\Models\Model;

class DummyClass
{
    public function all()
    {
        return Model::query()->get();
    }

    public function find($id)
    {
        return Model::query()->find($id);
    }

    public function create($data)
    {
        return $this->insert(new Model, $data);
    }

    public function update($id, $data)
    {
        $model = $this->find($id);

        return $this->insert($model, $data);
    }

    public function insert($model, $param = [])
    {
        $param = (object) $param;

        !isset($param->question) ?: $model->question = $param->question;

        $model->save();

        return $model;
    }
}